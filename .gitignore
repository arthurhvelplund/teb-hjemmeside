# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Editor / IDE
.vscode/
.idea/
*.sublime-*
*.swp
*.swo

# Node (hvis vi senere får brug for build-tools)
node_modules/
.npm/
.yarn/
package-lock.json

# Lokale arkiver / dev-junk
*.zip
*.tar.gz
tmp/

# Cowork / lokale sessions-mapper (hvis de ender her ved en fejl)
.claude/
local-agent-mode-sessions/

# Netlify CLI cache (hvis I senere bruger den lokalt)
.netlify/

# Logs
*.log

# Cloudflare / lokale secrets (kun dette projekt — må aldrig i git)
.env
.env.*
!.env.teb.example
.envrc
.wrangler/
*.pem
.gstack/
# Cloudflare lokal auth
.env.teb.local
# Agent tooling (lokalt / valgfrit at dele)
.grok/skills/
skills-lock.json
